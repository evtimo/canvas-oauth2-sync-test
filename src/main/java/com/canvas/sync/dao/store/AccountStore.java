package com.canvas.sync.dao.store;

import com.canvas.sync.dao.entity.AccountEntity;

public interface AccountStore extends BaseStore<AccountEntity> {
    // You can define custom query methods here if needed
}